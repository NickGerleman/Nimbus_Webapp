<% if flash[:errors] %>
form_methods.toggle_load();
if (!$('small').length)
    {
        $('#password').after('<small class="error">Incorrect Password</small> ');
    }
$('.row').addClass('error');
<% else %>
window.location.replace('<%= root_url %>');
<% end %>