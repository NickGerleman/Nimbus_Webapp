<% if flash[:errors] %>
stop_spinner();
if (!$('.error').length)
    {
        $('.modal #password').after('<small class="error">Incorrect Password</small> ');
    }
$('.modal .row').addClass('error');
<% else %>
window.location.replace('<%= logout_url %>');
<% end %>