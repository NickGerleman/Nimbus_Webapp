<% if flash[:errors] %>
stop_spinner();
if (!$('small').length)
    {
        $('#password').after('<small class="error">Incorrect Password</small> ');
    }
$('.row').addClass('error');
<% else %>
window.location.replace('<%= logout_url %>');
<% end %>