<% #Inline style tag is used for ruby injection of asset paths    %>
<style type="text/css">
    .wrapper {
        background: url(<%= image_path 'noise64.png'%>);
        background-size: 64px 64px;
    }

    @media (-webkit-min-device-pixel-ratio: 1.5), (min-resolution: 1.5 pppx) {
        .wrapper {
            background: url(<%= image_path 'noise128.png'%>);
            background-size: 64px 64px;

        }
    }

    #logo-image {
        background: url('<%= image_path 'banner.svg' %>') no-repeat;
        background-size: 210px 42px;
    }

</style>